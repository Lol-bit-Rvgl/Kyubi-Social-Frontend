name: kyubi
description: "Kyubi — Plataforma social de la comunidad, desarrollada por Nodo Apps."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  dio: ^5.11.0
  go_router: ^17.5.0
  flutter_secure_storage: ^10.3.1
  shared_preferences: ^2.5.5
  cached_network_image: ^3.4.1
  image_picker: ^1.2.3
  intl: ^0.20.3
  connectivity_plus: ^7.3.1
  socket_io_client: ^3.1.1
  url_launcher: ^6.3.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.9.0
  crypto: ^3.0.6
  web_socket_channel: ^3.0.3
  photo_view: ^0.15.0
  flutter_animate: ^4.5.2
  lottie: ^3.1.2
  confetti: ^0.7.0
  emoji_picker_flutter: ^3.0.0
  audioplayers: ^6.0.0
  mesh_gradient: ^1.3.8
  share_plus: ^10.0.0
  google_fonts: ^6.2.1
  firebase_core: ^4.14.0
  firebase_messaging: ^16.6.0
  firebase_crashlytics: ^5.3.0
  flutter_local_notifications: ^22.3.0
  just_audio: ^0.10.6
  video_player: ^2.14.0
  record: ^7.1.1
  youtube_player_iframe: ^6.0.2
  livekit_client: ^2.11.0
  permission_handler: ^11.3.1
  wakelock_plus: ^1.5.2
  google_sign_in: ^6.2.2
  gal: ^2.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.4
  build_runner: ^2.6.0
  freezed: ^3.2.5
  json_serializable: ^6.9.0

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icons/kyubi_logo.jpg"
  min_sdk_android: 21
  adaptive_icon_background: "#060609"
  adaptive_icon_foreground: "assets/icons/kyubi_logo.jpg"

flutter_native_splash:
  color: "#0A0A0B"
  image: assets/icon/app_icon.png
  image_dark: assets/icon/app_icon.png
  color_dark: "#0A0A0B"
  android: true
  ios: true
  android_12:
    color: "#0A0A0B"
    image: assets/icon/app_icon.png
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/animations/
    - assets/backgrounds/
    - assets/banners/
    - assets/icons/
    - assets/icons/amistad/
    - assets/images/
    - assets/icon/
    - assets/stickers/
    - assets/images/empty_states/
    - assets/images/onboarding/
    - assets/images/splash/
    - assets/images/avatars/
    - assets/images/profile/
    - assets/images/store/
    - assets/Emojis/Symbols/
    - assets/Emojis/Travel and places/
    - assets/Emojis/Smileys and emotions/
    - assets/Emojis/Objects/
    - assets/Emojis/People/
    - assets/Emojis/Food and drink/
    - assets/Emojis/Animals and nature/
    - assets/Emojis/Flags/
    - assets/Emojis/Activities and events/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

dependency_overrides:
  permission_handler_android: 12.0.13
